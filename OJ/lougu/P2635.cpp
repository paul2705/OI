#include<iostream>
#include<cstdio>
#include<cstring>
#include<algorithm>
using namespace std;
const int MAXN=3e3+5;
char a[MAXN],b[MAXN];
int ans,n,m;
int main(){
	cin>>a>>b;
	int n=strlen(a),m=strlen(b);
	int las=0,blas=0;
	for (int i=0;i<n;i++){
		if (a[i]=='*'||a[i]=='?'){
			for (int i
	return 0;
}
